[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# Fallback for Single Page Application routing (React SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
